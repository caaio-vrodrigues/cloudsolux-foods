spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  expiration: 3600000

cors:
  allowed-origins:
    - "http://localhost:3000"